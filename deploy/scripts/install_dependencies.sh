#!/bin/bash
set -e

echo "install_dependencies.sh started at $(date)"

# All system dependencies (Docker, AWS CLI, CodeDeploy agent)
# are installed via the Launch Template.


echo "install_dependencies.sh finished at $(date)"
